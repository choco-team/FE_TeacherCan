export type StyledButton = {
  color: 'primary' | 'secondary' | 'success' | 'warning' | 'error';
};

export type Button = {
  text: string;
  color: 'primary' | 'secondary' | 'success' | 'warning' | 'error';
};
